let Configs = {
  DEFAULT: {
    'ENV': 'DEV', // DEV, LIVE, STAGE
    'LOCALE': 'zh_CN'
  },

  Server: {
    'HOST': 'http://localhost',
    'CDN_HOST': 'http://localhost'
  }
};

module.exports = Configs;
